# MiniMind 代码详解（五）：强化学习详解

> 本篇讲解 DPO、GRPO 等强化学习方法，对应 `trainer/train_dpo.py` 和 `trainer/train_grpo.py`。

## 1. 为什么需要强化学习？

经过预训练和 SFT 后，模型已经能回答问题了。但：
- 回答可能不够准确
- 回答风格可能不符合人类偏好
- 可能生成有害内容

**强化学习的目标**：让模型输出更符合人类偏好的内容。

## 2. RLHF 的三个阶段

```
阶段1: 预训练 + SFT
      ↓
阶段2: 训练奖励模型（Reward Model）
   - 人类标注：哪个回答更好
   - 训练模型学习打分
      ↓
阶段3: 强化学习优化策略
   - PPO / DPO / GRPO
   - 让模型偏向高分回答
```

## 3. DPO（Direct Preference Optimization）

### 3.1 DPO 的核心思想

DPO 跳过了训练奖励模型，直接从偏好数据学习：

```
传统 RLHF:  偏好数据 → 奖励模型 → PPO优化
DPO:       偏好数据 → 直接优化
                      ↑
                   更简单！
```

### 3.2 DPO 损失函数

```python
# trainer/train_dpo.py 第33-51行
def dpo_loss(ref_log_probs, policy_log_probs, mask, beta):
    # 1. 计算每个序列的平均 log 概率
    seq_lengths = mask.sum(dim=1, keepdim=True).clamp_min(1e-8)
    ref_log_probs = (ref_log_probs * mask).sum(dim=1) / seq_lengths.squeeze()
    policy_log_probs = (policy_log_probs * mask).sum(dim=1) / seq_lengths.squeeze()

    # 2. 分离 chosen 和 rejected
    batch_size = ref_log_probs.shape[0]
    chosen_ref = ref_log_probs[:batch_size // 2]
    reject_ref = ref_log_probs[batch_size // 2:]
    chosen_policy = policy_log_probs[:batch_size // 2]
    reject_policy = policy_log_probs[batch_size // 2:]

    # 3. 计算 DPO 损失
    pi_logratios = chosen_policy - reject_policy    # 策略模型的偏好
    ref_logratios = chosen_ref - reject_ref         # 参考模型的偏好
    logits = pi_logratios - ref_logratios           # 相对变化

    loss = -F.logsigmoid(beta * logits)  # sigmoid 损失
    return loss.mean()
```

### 3.3 DPO 公式解读

```
DPO Loss = -log(σ(β * (log π(y_w|x)/π(y_l|x) - log π_ref(y_w|x)/π_ref(y_l|x))))

其中：
- π: 正在训练的策略模型
- π_ref: 参考模型（冻结的SFT模型）
- y_w: 好的回答（chosen）
- y_l: 差的回答（rejected）
- β: 温度参数，控制偏离程度
```

**直觉理解**：
- 让模型更偏向 chosen（增加其概率）
- 让模型远离 rejected（降低其概率）
- 但不能偏离 ref_model 太多（防止遗忘）

### 3.4 DPO 训练流程

```python
# trainer/train_dpo.py 第54-121行
def train_epoch(epoch, loader, ref_model, beta=0.1):
    for step, batch in enumerate(loader):
        # 1. 准备数据
        x_chosen = batch['x_chosen'].to(device)
        x_rejected = batch['x_rejected'].to(device)
        x = torch.cat([x_chosen, x_rejected], dim=0)
        y = torch.cat([y_chosen, y_rejected], dim=0)
        mask = torch.cat([mask_chosen, mask_rejected], dim=0)

        # 2. 计算参考模型的 log 概率（不更新梯度）
        with torch.no_grad():
            ref_logits = ref_model(x).logits
            ref_log_probs = logits_to_log_probs(ref_logits, y)

        # 3. 计算策略模型的 log 概率
        logits = model(x).logits
        policy_log_probs = logits_to_log_probs(logits, y)

        # 4. 计算 DPO 损失并更新
        loss = dpo_loss(ref_log_probs, policy_log_probs, mask, beta)
        loss.backward()
        optimizer.step()
```

### 3.5 log_probs 计算

```python
# trainer/train_dpo.py 第24-30行
def logits_to_log_probs(logits, labels):
    # logits: [batch, seq_len, vocab_size]
    # labels: [batch, seq_len]

    log_probs = F.log_softmax(logits, dim=2)  # 转成 log 概率
    # 取出每个位置真实标签对应的 log 概率
    log_probs_per_token = torch.gather(log_probs, dim=2, index=labels.unsqueeze(2)).squeeze(-1)
    return log_probs_per_token  # [batch, seq_len]
```

**直觉**：
- 模型对一个序列的"概率"= 每个 token 概率的乘积
- log 概率的好处：乘法变加法，数值更稳定

## 4. GRPO（Group Relative Policy Optimization）

### 4.1 GRPO 的核心思想

GRPO 是 DeepSeek-R1 使用的算法，不需要单独的奖励模型：

```
传统 PPO:  策略模型 + 价值模型 + 奖励模型（3个模型）
GRPO:     策略模型 + 参考模型 + 组内相对奖励（2个模型）
                               ↑
                         关键创新！
```

### 4.2 GRPO 的工作流程

```
1. 给同一个 prompt 生成 N 个回答（num_generations=8）
           ↓
2. 用奖励模型给每个回答打分
           ↓
3. 计算组内相对优势（Advantage）
   - 分数高于平均 → 正优势 → 增加概率
   - 分数低于平均 → 负优势 → 降低概率
           ↓
4. 用策略梯度更新模型
```

### 4.3 奖励计算

```python
# trainer/train_grpo.py 第27-92行
def calculate_rewards(prompts, responses, reward_model, reward_tokenizer):
    rewards = torch.zeros(len(responses), device=device)

    # 1. 格式奖励（推理模型专用）
    if reasoning:
        # 检查是否有正确的 <think></think><answer></answer> 格式
        pattern = r"^<think>\n.*?\n</think>\n<answer>\n.*?\n</answer>$"
        for i, response in enumerate(responses):
            if re.match(pattern, response, re.S):
                rewards[i] += 0.5  # 格式正确加分

            # 标签数量检查
            if response.count("<think>") == 1: rewards[i] += 0.25
            if response.count("</think>") == 1: rewards[i] += 0.25
            if response.count("<answer>") == 1: rewards[i] += 0.25
            if response.count("</answer>") == 1: rewards[i] += 0.25

    # 2. 奖励模型打分
    for i, (prompt, response) in enumerate(zip(prompts, responses)):
        score = reward_model.get_score(reward_tokenizer, messages + [{"role": "assistant", "content": response}])
        score = max(min(score, 3.0), -3.0)  # 限制范围
        rewards[i] += score

    return rewards
```

### 4.4 优势函数计算

```python
# trainer/train_grpo.py 第133-137行
# 计算组内相对优势
grouped_rewards = rewards.view(-1, num_generations)  # [B, 8]

mean_r = grouped_rewards.mean(dim=1)  # 组内平均
std_r = grouped_rewards.std(dim=1)    # 组内标准差

# 标准化优势
advantages = (rewards - mean_r) / (std_r + 1e-4)
advantages = (advantages - advantages.mean()) / (advantages.std() + 1e-8)
```

**直觉**：
```
假设 8 个回答的分数是：[3, 5, 2, 8, 4, 6, 1, 7]
平均分：4.5
标准差：2.29

优势值：
回答1: (3-4.5)/2.29 = -0.65 → 降低概率
回答2: (5-4.5)/2.29 = +0.22 → 略微提升
回答4: (8-4.5)/2.29 = +1.53 → 大幅提升
回答7: (1-4.5)/2.29 = -1.53 → 大幅降低
```

### 4.5 策略损失计算

```python
# trainer/train_grpo.py 第139-148行
# 1. 计算 KL 散度惩罚
kl_div = ref_per_token_logps - per_token_logps
per_token_kl = torch.exp(kl_div) - kl_div - 1  # 近似 KL

# 2. 计算策略梯度损失
per_token_loss = -(
    torch.exp(per_token_logps - per_token_logps.detach()) * advantages.unsqueeze(1)
    - beta * per_token_kl  # KL 惩罚
)

# 3. 只在有效 token 上计算
policy_loss = ((per_token_loss * completion_mask).sum(dim=1) / completion_mask.sum(dim=1)).mean()
```

### 4.6 GRPO 公式

```
L_GRPO = -E[ Σ_t ( exp(log π(a_t|s_t) - log π_old(a_t|s_t)) * A_t ) - β * KL(π || π_ref) ]

其中：
- π: 策略模型（正在更新）
- π_old: 策略模型（更新前，detach）
- π_ref: 参考模型（冻结）
- A_t: 优势值
- β: KL 惩罚系数
```

## 5. PPO 简介

MiniMind 也实现了 PPO（`train_ppo.py`），这是更传统的 RLHF 方法：

```
PPO 的组件：
1. Actor（策略模型）：生成回答
2. Critic（价值模型）：估计状态价值
3. Reward Model：打分

PPO 的优化目标：
- 最大化奖励
- 限制策略更新幅度（clip）
- 价值函数逼近
```

**PPO vs GRPO**：
| 方面 | PPO | GRPO |
|------|-----|------|
| 模型数量 | 3个 | 2个 |
| 显存占用 | 高 | 低 |
| 实现复杂度 | 高 | 低 |
| 效果 | 好 | 相当 |

## 6. 推理模型训练（train_reason.py）

MiniMind 还支持训练类似 DeepSeek-R1 的推理模型：

### 6.1 推理模型的特点

```
普通模型回答：
User: 1+1=?
Assistant: 1+1=2

推理模型回答：
User: 1+1=?
Assistant: <think>
让我来计算这个加法问题。
1+1 是一个基本的加法运算。
1 加上 1 等于 2。
</think>
<answer>
1+1=2
</answer>
```

### 6.2 格式奖励的作用

```python
# trainer/train_grpo.py 第29-52行
def reasoning_model_reward(rewards):
    # 检查格式是否正确
    pattern = r"^<think>\n.*?\n</think>\n<answer>\n.*?\n</answer>$"

    for i, response in enumerate(responses):
        if re.match(pattern, response, re.S):
            rewards[i] += 0.5  # 格式正确 +0.5

        # 标签完整性检查
        if response.count("<think>") == 1: rewards[i] += 0.25
        if response.count("</think>") == 1: rewards[i] += 0.25
        if response.count("<answer>") == 1: rewards[i] += 0.25
        if response.count("</answer>") == 1: rewards[i] += 0.25

    return rewards  # 最高可得 1.5 分格式奖励
```

## 7. 强化学习的关键参数

### 7.1 DPO 参数

```python
# trainer/train_dpo.py 参数
--beta 0.1           # KL 惩罚系数（越大越保守）
--learning_rate 4e-8 # 学习率（非常小！）
--batch_size 4       # 小 batch（显存限制）
```

### 7.2 GRPO 参数

```python
# trainer/train_grpo.py 参数
--num_generations 8  # 每个prompt生成8个回答
--beta 0.02          # KL 惩罚系数
--max_gen_len 1536   # 最大生成长度
--learning_rate 8e-8 # 学习率
```

### 7.3 为什么强化学习的学习率这么小？

```
预训练学习率：   5e-4
SFT 学习率：     1e-6
DPO/GRPO 学习率：4e-8 ~ 8e-8

原因：
1. 模型已经很好了，只需要微调
2. 强化学习信号噪声大，容易过拟合
3. 防止灾难性遗忘
```

## 8. 实践建议

### 8.1 选择哪种方法？

| 场景 | 推荐方法 | 原因 |
|------|----------|------|
| 有偏好数据 | DPO | 简单高效 |
| 要训练推理模型 | GRPO | 支持格式奖励 |
| 显存充足 | PPO | 效果最好 |
| 快速实验 | DPO | 不需要奖励模型 |

### 8.2 数据准备

**DPO 数据**：
```json
{
  "chosen": [问题 + 好回答],
  "rejected": [问题 + 差回答]
}
```

**GRPO 数据**：
```json
{
  "conversations": [问题]  // 只需要问题，回答由模型生成
}
```

### 8.3 训练流程

```bash
# 1. 先完成预训练和 SFT
python train_pretrain.py
python train_full_sft.py

# 2. DPO 训练
python train_dpo.py --from_weight full_sft

# 或 GRPO 训练（需要奖励模型）
python train_grpo.py --from_weight full_sft --reward_model_path /path/to/reward_model
```

## 9. 总结

| 方法 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| DPO | 直接从偏好数据学习 | 简单、无需奖励模型 | 需要成对数据 |
| GRPO | 组内相对优势 | 效率高、支持推理 | 需要奖励模型 |
| PPO | Actor-Critic | 效果好 | 复杂、显存高 |

**核心公式**：
- DPO: 最大化 log(σ(β * Δ))，其中 Δ = 策略偏好 - 参考偏好
- GRPO: 最大化 A * log π - β * KL，其中 A 是组内相对优势

---

[← 上一篇：训练流程详解](./04_训练流程详解.md)

---

## 附录：学习路线图

```
第1周：理解基础
├── 阅读 01_LLM基础概念.md
├── 阅读 02_模型架构详解.md
└── 运行 eval_llm.py 测试模型

第2周：理解数据和训练
├── 阅读 03_数据处理详解.md
├── 阅读 04_训练流程详解.md
└── 运行 train_pretrain.py（小数据集）

第3周：动手训练
├── 下载完整数据集
├── 完成预训练 + SFT
└── 测试自己训练的模型

第4周：进阶
├── 阅读 05_强化学习详解.md
├── 尝试 LoRA 微调
└── 尝试 DPO 训练

持续学习：
├── 阅读 MiniMind 源码
├── 修改模型结构做实验
└── 在自己的数据上训练
```

**祝你学习愉快！** 🎉
